# 網站狀態監控與自動截圖工具 (checkWeb) 操作手冊

---

## 1. 系統簡介與核心功能

**checkWeb** 是一套基於 Python Streamlit 與 Playwright 無頭瀏覽器技術開發的自動化網站健康檢查與截圖報表系統。系統能自動化針對清單中的各目標網站發送 HTTP 請求檢測連線狀態、計算回應時間 (Latency)，並自動利用真實 Chromium 瀏覽器進行即時網頁畫面截圖，最終一鍵輸出為 CSV 彙總數據與包含各網站截圖之正式 Word (.docx) 檢測報告。

### 🌟 核心功能一覽
* **多目標網址批次檢測**：自動讀取 `urlList.txt` 中的目標網站清單，批次執行 HTTP GET 狀態碼與回應時間 (ms) 檢測。
* **Playwright 無頭瀏覽器截圖**：透過 Chromium 於背景無頭模式渲染目標網頁，支援等待動態 JavaScript 載入，並截取 1280x720 預覽圖。
* **繁體中文字體支援**：容器環境預裝 `fonts-noto-cjk` 字型庫，徹底避免中文網頁截圖出現方框亂碼 (`□`)。
* **即時視覺化進度與卡片預覽**：Streamlit 互動介面提供檢測進度條、即時狀態表格與 3 欄式網頁截圖預覽。
* **多格式報告即時匯出**：
  * **CSV 報告**：包含網站名稱、URL、測試時間、狀態、回應毫秒數與 HTTP 代碼。
  * **Word (.docx) 完整報告**：包含總結摘要表格，並逐頁附上各網站高解析度截圖，適合直接作為每日或定時巡檢報表存檔。
* **多平台雲端適配**：
  * 支援 **Render.com**（Docker 容器化、新加坡極低延遲節點、原生健康檢查）。
  * 支援 本地開發與單機快速執行。

---

## 2. 專案目錄架構與模組清單

```text
checkWeb/
├── Dockerfile                  # Render.com 專用容器建置檔 (內建 Playwright + 中文字型)
├── render.yaml                 # Render Blueprint 基礎設施即代碼 (IaC) 配置檔
├── requirements.txt            # Python 依賴套件清單 (含 Streamlit, Pandas, Playwright)
├── packages.txt                # Linux apt 系統相依套件清單 (備份參考)
├── app.py                      # Streamlit 核心應用程式 (UI、監測邏輯、Playwright 截圖)
├── urlList.txt                 # 預設待檢測之網站名稱與網址清單
├── 操作手冊.md                  # 系統完整操作手冊 (本手冊)
├── render_deploy_guide.md      # Render.com 雲端詳細部署指南
├── DEPLOYMENT_GUIDE.md         # 舊版 Hugging Face 部署指引
├── README.md                   # 專案簡介
├── screenshots/                # 執行過程產生的各網站臨時截圖目錄 (自動生成)
└── docs/
    └── ai_planning/            # 架構設計與版本遷移規劃紀錄
        └── render_deployment_plan.md
```

### 關鍵模組說明
* **`app.py`**：
  * `load_urls(filename)`：解析 `urlList.txt`，萃取名稱與 URL。
  * `check_website(url)`：使用 `requests` 檢測狀態碼與計算回應時間 (ms)。
  * `capture_screenshot(url, filename)`：使用 `playwright.async_api` 無頭瀏覽器載入網頁並截圖存檔。
  * `create_docx(df)`：使用 `python-docx` 動態產生彙總表格與各網站排版截圖之 Word 文件。
* **`Dockerfile`**：
  * 採用 `python:3.10-slim`，整合 Debian 函式庫、中文字型與 Chromium 瀏覽器，解決無 GUI 伺服器截圖核心問題。

---

## 3. 環境建置與依賴安裝步驟

### 3.1 本地開發環境需求
* **Python**：3.10 或 3.11 版本
* **作業系統**：Windows / macOS / Linux

### 3.2 本地安裝依賴
開啟終端機，執行以下步驟：
```powershell
# 1. 進入專案目錄
cd d:\MyLab\checkWeb

# 2. 安裝 Python 依賴套件
pip install -r requirements.txt

# 3. 安裝 Playwright 核心瀏覽器 (首次執行必備)
playwright install chromium
```

---

## 4. 組態設定說明 (Config & Settings)

### 4.1 檢測目標設定檔 (`urlList.txt`)
格式採用「名稱」與「網址」成對交替排列：
```text
基金相關系統驗證網址:
TAQ
https://taq.tdcc.com.tw/QRY/mainc.jsp

FDATA
https://fdata.fundclear.com.tw/fdata/viewLogin.action

基金觀測站
https://www.fundclear.com.tw/
```
* 第一行若非 `http` 開頭視為備註或分類名稱。
* 後續奇數行為**網站名稱**，偶數行為**目標網址**（需帶 `http://` 或 `https://`）。

### 4.2 雲端環境變數 (Render.com)

| 變數名稱 (Key) | 建議值 | 說明 |
| :--- | :---: | :--- |
| `PYTHONUNBUFFERED` | `1` | 確保 Python standard output 即時輸出日誌 |
| `STREAMLIT_SERVER_HEADLESS` | `true` | 強制 Streamlit 於雲端無頭背景運行 |
| `STREAMLIT_BROWSER_GATHER_USAGE_STATS` | `false` | 關閉 Streamlit 遙測統計，提升執行效能 |

---

## 5. 操作指令與使用範例

### 5.1 本機啟動與測試
```powershell
# 啟動 Streamlit 本地伺服器
streamlit run app.py
```
啟動後瀏覽器會自動開啟 `http://localhost:8501`。

### 5.2 Docker 容器本機測試
```bash
# 1. 構建映像檔
docker build -t check-web .

# 2. 啟動容器並映射端口 10000
docker run -d -p 10000:10000 -e PORT=10000 check-web
```
開啟瀏覽器訪問 `http://localhost:10000` 即可測試運行。

### 5.3 Render.com 雲端 Blueprint 一鍵部署
1. 將代碼推送至 GitHub 倉庫：
   ```powershell
   git add .
   git commit -m "feat: configure render deployment support for checkWeb"
   git push origin main
   ```
2. 前往 [Render Dashboard](https://dashboard.render.com/)，點選 **"New +"** -> **"Blueprint"**。
3. 選取 **`checkWeb`** 倉庫，Blueprint Name 輸入 **`check-web`**。
4. 點擊 **"Apply"**，Render 即會自動啟動 Docker 打包與部署。
5. 部署完成後訪問：`https://check-web.onrender.com`。

---

## 6. 輸出結果與報表解讀

### 6.1 網頁即時狀態表
| 欄位名稱 | 說明 | 範例值 |
| :--- | :--- | :--- |
| **名稱** | 目標網站名稱 | `TAQ` |
| **網址** | 監測目標網址 | `https://taq.tdcc.com.tw/...` |
| **測試時間** | 台灣時區 (UTC+8) 測試時間 | `2026-09-03 10:00:00` |
| **狀態** | 判定結果 | `✅ 正常` 或 `❌ 異常 (500)` 或 `⚠️ 無法連線` |
| **回應時間 (ms)** | HTTP 請求耗時 | `124.52` |
| **代碼** | HTTP 狀態碼 | `200` |

### 6.2 匯出報表說明
1. **`website_status_report.csv`**：
   - 採用 UTF-8 BOM (`utf-8-sig`) 編碼，Excel 開啟不會產生中文亂碼。
2. **`website_report_YYYYMMDD_HHMMSS.docx`**：
   - 第一頁為統整狀態網格清單。
   - 分頁後依序列出各網站資訊與高解析度網頁截圖。

---

## 7. 常見問題排解 (FAQ & Troubleshooting)

### Q1：截圖中的中文字體顯示為方框或問號 (`□`)？
* **原因**：Linux 系統缺乏中文字型庫。
* **解法**：專案提供的 `Dockerfile` 已預裝 Debian 繁體中文字型套件 `fonts-noto-cjk`，在 Render.com Docker 容器環境下截圖能 100% 正確呈現繁體中文。

### Q2：Render 免費版啟動時較慢或休眠？
* **原因**：Render.com 免費實例 15 分鐘無人連線會自動休眠。
* **解法**：使用外部定時工具（如 [UptimeRobot](https://uptimerobot.com/) 或 [cron-job.org](https://cron-job.org/)），設定每 10 分鐘向 `https://check-web.onrender.com/_stcore/health` 發送 GET 請求，即可保持容器 24 小時秒開。

### Q3：某些內部網站截圖失敗或出現連線逾時？
* **原因**：部分網站設有海外 IP 存取限制、防火牆或頻繁防爬蟲機制。
* **解法**：程式中已配置 Chromium 偽裝 User-Agent、忽略 SSL 憑證檢測 (`verify=False`) 與 45 秒載入逾時容錯。若特定網站僅限台灣本地 IP 訪問，雲端伺服器 (新加坡節點) 可能受到目標站防火牆阻擋。
